# Bab 2 - Tampilan: Fondasi, Tipografi, Layar, dan HUD

Seluruh antarmuka game - menu, HUD, bag, profil - digambar sebagai quad di dalam
kanvas Three.js. Tidak ada elemen HTML di dalam game: satu `<canvas>` saja
(`index.html`), dengan `cursor: none` supaya kursor sistem tidak muncul dobel.

## 2.1 Skala: satu angka untuk seluruh layar

`[CODE]` `src/ui3/screen.js:536`

```js
function fitScale() {
  const fill = Math.max(0.5, Math.min(window.innerWidth / C.W,
                                      window.innerHeight / C.H));
  const whole = Math.floor(fill / C.RS) * C.RS;
  return whole >= MIN_SCALE ? whole : fill;
}
```

Aturannya: cari kelipatan **bulat** dari `C.RS` (= 2) yang terbesar dan masih
muat, jadi setiap piksel art jatuh tepat di jumlah piksel layar yang sama dan teks
tetap tajam. Kalau jendela lebih kecil dari `MIN_SCALE` (= `C.RS`), frame diisi
fraksional daripada menjadi lebih kecil dari art-nya. Frame hasilnya **diletakkan
di tengah layar** (`view.x`, `view.y`) alias letterbox.

Angka nyata pada monitor FullHD 1920x1080: `fill = min(6, 6) = 6`, `whole = 6` →
**skala 6**, frame = 1920x1080 penuh tanpa letterbox. Pada jendela 1280x720:
`fill = 4`, frame 1280x720. Itulah mengapa game tidak lagi perlu di-zoom 200%.

> **Kenapa ini penting.** Sebelum v3.1.0, nilai skala dihitung di dua tempat
> (renderer dan lapisan layar) dan pemain harus men-zoom browser sampai 200% supaya
> UI-nya pas. Sekarang `fitScale()` adalah pemilik tunggal: `renderer3d` bertanya ke
> sini untuk viewport dunianya, `DS.R.fitScale` meneruskannya, dan kursor memakai
> angka ini untuk menerjemahkan posisi mouse.

## 2.2 Grid layar, sebenarnya

Bab ini di versi sebelumnya mendokumentasikan `src/ui/kit.js` sebagai "grid + komponen
+ audit tata letak" yang wajib dipakai semua layar. Itu **tidak pernah terjadi**: kit
terdaftar di `index.html` tapi tidak pernah dipanggil satu kali pun, dan sekarang
filenya sudah dihapus (390 baris). Aturan yang benar-benar berlaku hari ini:

- **Frame**: satu kotak 320x180 unit logis, diskalakan oleh `fitScale()` (Bagian 2.1)
  dan dipetakan ke jendela oleh `DS.UI3.view`. Setiap layar menggambar di koordinat
  logis ini; tidak ada satuan lain.
- **Tepi aman**: tiap layar menjaga **margin 4 unit** dari tepi frame, dan
  menggantikan band bawah untuk hint (`y = C.H - 14`) serta band judul atas
  (`y = 4..44`). Angka ini diulang di tiap layar secara sadar, bukan lewat helper.
- **Konten 3D di dalam frame**: `DS.UI3.addPrePass(scene, camera)` menyisipkan
  sebuah scene Three.js ke dalam frame (setelah clear depth, sebelum UI), dan daftar
  itu dikosongkan setiap frame — jadi layar yang lupa mencabutnya tidak bocor ke
  frame berikutnya. Dipakai oleh diorama menu dan boneka profil.
- **Verifikasi**: tata letak diukur, bukan dilihat — `npm run qa:frame` mengukur
  frame di 9 ukuran jendela, `npm run qa:menu` mengukur isi frame menu dari piksel,
  dan `npm run qa:hud` + `npm run qa:paint` mengukur tabel letak HUD lalu frame
  terkompositnya di 3 ukuran jendela (lihat 2.7).

> **Catatan kerja.** Kalau grid bersama dibutuhkan lagi, bentuknya harus dimulai dari
> komponen yang **benar-benar dipakai** dua layar atau lebih. Kit sebelumnya adalah
> contoh cara sebaliknya: satu set komponen rapi yang tidak pernah dipanggil.

## 2.3 Post-process: fade, flash, vignette, dan `dim`

`[CODE]` `src/ui3/screen.js:425` menyimpan keadaan post di satu objek:

| Field | Fungsi |
|---|---|
| `fade` + `fadeColor` | Menutup seluruh frame dengan warna (cutscene, mati, ganti lantai). |
| `flash` + `flashColor` | Kilatan putih/kuning sesaat (ledakan, kena pukul). |
| `tint` | Nuansa warna seluruh layar. |
| `vignette` | Pengerasan tepi frame; naik tipis dengan kedalaman (`0.18 + horror*0.22`). |
| `grain` | Bintik film saat lantai dalam (`horror*0.16`). |
| `darken` | Penggelap penuh; sudah tidak dipakai lagi sejak fitur "ruangan gelap" dihapus. |
| `dim` + `dimColor` | **Bukan** post: ini digambar antara dunia dan UI, sehingga panel tetap terang di atas dunia yang menggelap. |

> **Catatan bug yang pernah terjadi dan pelajarannya.** Sebelum ada `dim`, layar
> bag dan profil memakai `fade`, yang dijalankan sesudah semua UI - akibatnya
> panelnya sendiri ikut menggelap 74-78% (terukur `[RUNTIME]`) dan tampak hitam.
> Aturannya sekarang: `fade` hanya untuk menutup **seluruh** frame (cutscene/mati),
> `dim` untuk membuat latar belakang sebuah panel mundur.

Satu jebakan teknis yang pernah memakan waktu: kamera UI memakai Y terbalik
(0 di atas, H di bawah), sehingga orientasi segitiga terbalik dan semua batch UI
harus memakai `side: DoubleSide`. Quad `dim` pertama lupa itu dan ter-cull
(warnanya tidak muncul sama sekali meskipun nilainya di-set).

## 2.4 Tipografi: tiga bitmap face

`[CODE]` `src/art/font3.js` **dihasilkan** oleh `tools/art/font3.py`. Tiga face:

| Face | Atlas | Sel logis | Dipakai untuk |
|---|---|---|---|
| `MICRO` | 6x10 px | 3x5 | Keycap, label kecil, angka koordinat, tag rarity |
| `BODY` | 10x14 px | 5x7 | Prosa: nama item, deskripsi, isi menu, HUD |
| `TITLE` | 20x28 px | skala penuh | Judul layar dan judul menu utama |

Dua metrik yang menjadi kontrak dan **tidak boleh berubah** karena hampir semua
posisi HUD bergantung padanya: `R.textWidth(str)` (BODY) dan
`R.textSmallWidth(str)` (MICRO).

Face `TITLE` diberi "serif blast" - kaki lebar pada huruf kapital, bahu sempit di
atas, dan perpanjangan batang di bawah baseline - sehingga judul terbaca seperti
batu terukir, bukan prosa yang diperbesar. Face `MICRO` dulu diperhalus dengan
Scale2x/EPX, dan hasilnya `SHIFT` terbaca `SHIFY`; sekarang pembesaran memakai
NEAREST sehingga diagonal satu piksel menjadi blok 2x2 yang bersih.

Gaya huruf: seluruh teks adalah **kapital** (`uppercase`) karena font bitmap-nya
memang dirancang begitu. Tidak ada font sistem yang dipakai di dalam kanvas.

## 2.5 Main menu

`[CODE]` `src/scenes/menu.js:217`

| Item | Aksi |
|---|---|
| `START RUN` | Loker nama (kalau belum ada) lalu layar pilih senjata (`loadout`) |
| `HOW TO PLAY` | Layar kontrol & penjelasan |
| `RECORDS` | Tabel rekor dari `localStorage` |
| `MUTE` / `UNMUTE` | Label berubah sesuai keadaan audio |

Tata letak (unit logis):

| Elemen | Posisi |
|---|---|
| Kolom menu | `MENU_X = 16`, `MENU_Y = 74`, `MENU_W = 130`, `MENU_H = 16` per baris |
| Peringatan | `ONE LIFE PER RUN` di `MENU_X+2`, `MENU_Y + 4*16 + 6` |
| Judul | `DUNGEON` di tengah, `y = 8` (skala 3, warna emas), `SCROLLING` di `y = 30` (skala 2, ungu `#c86ee0`) |
| Wash gelap di belakang judul | `rect(0, 4, 320, 40)` warna `rgba(7,11,12,0.55)` |
| Diorama | **3D** (`src/scenes/camp3d.js`): hero voxel di api unggun voxel, 30 pohon tiga kedalaman, kunang-kunang, kelalawar, kamera bernafas. Diumpankan lewat `DS.UI3.addPrePass` |
| Fallback 2D | Hutan dilukis: `HERO_X = 168`, `HERO_Y = 94`, `FIRE_X = 232`, `FIRE_Y = 100` — dipakai hanya kalau diorama belum bisa dibangun pada frame itu |

Interaksi: hover menggerakkan pilihan (dan memainkan `menuMove`), klik/Enter
memilih, panah atas-bawah memindahkan kursor. Layar selalu dimulai di scene
`DS.Scenes.menu()`.

![Main menu: empat entri, kamp di kanan, peringatan satu-nyawa di bawah](docs/img/menu.png)

## 2.6 Layar lain di menu

### NAME (nama pemain)

`[CODE]` `src/scenes/menu.js` — muncul sekali, pada START RUN pertama, sebelum pilih
senjata; setelah nama diterima, tidak pernah ditanya lagi.

| Hal | Nilai |
|---|---|
| Penyimpanan | `localStorage['ds_name']`, lewat `DS.Board.setName()` |
| Aturan nama | 2-12 karakter ASCII yang bisa dicetak, spasi dirapikan (`DS.Board.clean`) |
| Input teks | `DS.Input.setTextSink(fn)`: selama sink terpasang, tombol huruf dan Backspace dikirim ke sink dan **dikonsumsi**, jadi mengetik nama tidak ikut melompat/dash. Enter dan Escape tetap `confirm`/`back` |
| Kotak isi | `x = (320-180)/2`, `y = 92`, `w = 140`, `h = 14`; caret berkedip `_` setiap 40 frame |
| Nama gagal | Pesan merah `TWO TO TWELVE CHARACTERS` di `y = 108`, frame input jadi merah |
| Nama dipakai di | label di atas kepala MC (2D *dan* mode voxel), baris `RANK #n` di layar kematian, dan kolom nama di ladder |

![Layar nama: diminta sekali, disimpan di localStorage](docs/img/name.png)

Nama ini juga yang dikirim ke ladder online — lihat Bagian 8.12 (Worker + D1).

### HOW TO PLAY

Satu layar, dikelompokkan: gerak, tempur, dan item - masing-masing berupa baris
`keycap + aksi`. Isinya harus konsisten dengan `CONTROL_ROWS` di Bab 3.4.

![How to play](docs/img/menu-help.png)

### RECORDS

`[CODE]` Membaca `DS.Storage.load()` dan menampilkan: `BEST DEPTH`, `RUNS`,
`TOTAL KILLS`, item terbaik (`bestItemName` + warna rarity-nya), dan
`FASTEST CLEAR` (format `mm:ss` dari `fastestClearFrames`). Tombol `RUN AGAIN`
masuk ke `DS.Scenes.loadout()` (langsung pilih senjata, intro di-skip), `MENU`
kembali ke menu utama.

![Records: rekor dibaca dari localStorage](docs/img/menu-records.png)

### LOADOUT (pilih senjata)

`[CODE]` `src/scenes/menu.js:322` - enam pilihan dengan urutan
`sword, dagger, spear, greataxe, bow, staff`, disusun **3 kolom** (`cw=92`,
`ch=26`, `gapX=4`, `gapY=4`, di tengah frame). Setiap kartu menampilkan nama,
`blurb`, statistik inti, dan skill/ult yang akan didapat. Tawaran selalu versi
**paling polos** dari archetype itu (rarity common, tanpa affix, tanpa elemen),
supaya pilihan menentukan *cara main*, bukan kekuatan awal.

![Weapon select: enam archetype, ketiganya di baris pertama](docs/img/loadout.png)

### Intro (cutscene pembuka)

`[CODE]` `src/scenes/cutscene.js` - empat fase dengan panjang frame tetap:

| Fase | Frame | Isi |
|---|---|---|
| `WALK` | 170 | Hero jalan, koridor bergulir (`SCROLL = 1.15` px/frame) |
| `EDGE` | 62 | Berhenti di tepi, lantai retak |
| `FALL` | 118 | Jatuh |
| `LAND` | 46 | Mendarat di lantai 1 |

Total 396 frame (~6,6 detik) dan bisa di-skip. Dunia digambar dengan tile dan art
hero yang sama dengan gameplay (tidak ada aset khusus cutscene). Lantai 1 memakai
biome `DS.Biomes.forDepth(1)`.

![Cutscene pembuka: hero, koridor, dan lantai yang tidak menahan](docs/img/intro.png)

### Game over & cleared

`[CODE]` `src/scenes/menu.js` (`createGameOver`) - judul merah `YOUR RUN ENDS HERE`
saat mati dan emas `THE DUNGEON IS CLEARED` saat menang. Panelnya **dua kolom**
(`x = 20`, `y = 46`, `w = 280`, `h = 96`):

| Kolom | Isi |
|---|---|
| Kiri (`x = 26`, nilai rata kanan di `x = 150`) | `DEPTH REACHED`, `ENEMIES SLAIN`, `COINS`, `TIME`, `BEST WEAPON` |
| Kanan (`x = 168`, pemisah di `x = 160`) | `LADDER`: 8 baris `#rank nama depth`, baris milik pemain sendiri diberi warna emas, plus label sumber `ONLINE` / `THIS DEVICE` |
| Bawah tengah (`y = 148`) | `NAMA   RANK #n` |
| Atas (`y = 34`) | Badge `NEW RECORD` berkedip, kalau rekor run ini terlewati |

Di sinilah `Storage.recordRun()` dipanggil (tepat sekali per run) dan
`DS.Board.submit()` mengirim run ke ladder. Keduanya **tidak** ditunggu: layar ini
muncul dulu, lalu baris ladder diisi saat jawaban datang — kalau `/api/*` tidak
bisa dihubungi, run disimpan lokal dan labelnya berubah jadi `THIS DEVICE`.

![Mati di tengah run: judul merah dan ringkasan run](docs/img/gameover.png)

![Menang: THE DUNGEON IS CLEARED, dengan badge NEW RECORD bila rekor terlewati](docs/img/cleared.png)

Profil punya tiga tab, semuanya digambar di dalam frame yang sama dan sengaja
memakai gutter kanan yang direservasi supaya tidak pernah tertutup plate mata
uang:

![Tab CHARACTER: armor yang dipakai dan statistik turunannya](docs/img/profile-character.png)

![Tab STATS](docs/img/profile-stats.png)

![Tab RUN: keadaan run yang sedang berjalan](docs/img/profile-run.png)

## 2.7 HUD saat bermain

`[CODE]` `src/ui/ui.js` menyusun HUD dalam tata letak action-RPG: vital di
bawah tengah, kemampuan di kanan bawah, mata uang di kanan atas, dan hanya nama
lantai di atas.

**Satu tabel, satu pemilik.** `hudBoxes(g)` adalah satu-satunya tempat letak setiap
klaster diputuskan; `hud()` menyerahkan kotak yang cocok ke setiap fungsi gambar,
dan `DS.UI.hudBoxes` diekspor supaya bisa diukur. Dua angka yang menurunkan
semuanya:

- `HUD_GUTTER = 4` — jarak dari tepi frame ke piksel HUD terdekat.
- `HUD_BAND = 27` — tinggi satu baris yang dipakai bersama tiga klaster bawah,
  sehingga vital, tangan, dan tile skill berdiri di satu garis dasar, bukan tiga.

Sebelum v5.2.1 tiap klaster menulis koordinatnya sendiri. Itu sebabnya mata uang
78 unit dengan baris 16x16 mengelilingi koin 6x6, keycap skill tergantung lewat
tepi bawah frame, bar boss digambar di atas bilah HP, dan kartu kontrol menutupi
banner kedalaman pada detik pertama run. Semua angka itu masuk akal sendiri-sendiri;
jumlahnya yang tidak.

| Klaster | Kotak (`x, y, w, h`) | Isi |
|---|---|---|
| Banner lantai | `4, 3, 270, 8` | `DEPTH n` / `SAFE ROOM` / `THRONE ROOM` / `THE TRIAL`. Teks **ditengahkan pada frame** (`C.W/2`), bukan pada kotak: titik tengah kotak 21 unit di kiri tengah frame. Label terpanjang (`THRONE ROOM`, 65 unit) selalu masuk |
| Mata uang | `276, 3, 40, 27` (39 dengan kunci) | Baris koin / shard / kunci. `rowH = 9`, `gap = 3`, `pad = 3`; tinggi `6 + 9*rows + 3*(rows-1)`. Ikon 6x6-7x8 ditengahkan dalam well 9x9 |
| Boon | `305, plate.h + 6, 11, ±110` | Kolom chip 8x8 di tepi kanan; kalau daftar lebih panjang dari kolom, sisanya jadi `+n` |
| Vital | `110, 149, 120, 27` | Bilah HP 7 px dengan angka di dalamnya, haarline shield 3 px, MP+SP 5 px bersebelahan (label **dan** nilai di dalam bilah), baris pip dash dengan label `DASH` |
| Tangan + armor | `4, 149, 60, 27` | Dua slot 18x18 (keycap `1`/`2` di baris atas), pembatas, lalu 3 chip armor 12x5 bertumpuk |
| Skill | `276, 149, 40, 27` | Dua tile 18x18 (`E` = skill, `X` = ult); keycap di baris atas, cooldown sebagai shutter, harga mana di pojok kanan bawah tile |
| Momentum | `122, 13, 76, 18` | `x30` di baris atas, bilah decay, lalu tier + `+n%` berbagi baris bawah (masing-masing dipotong ke sisa ruangnya) |
| Boss bar | `90, 34, 140, 16` | Nama (dipotong ke kotak) + bilah 6 px dengan tick per kuarter; `ENRAGED` jadi tag di ujung kiri bilah, bukan kata setelah nama |
| Breath | `126, 52, 68, 20` | Label, bilah, dan `DROWNING`; hanya saat di air atau sesaat setelah muncul |
| Toast | `60, 76, 200, 8` | Satu baris pesan; naik maksimal 1 unit saat memudar |
| Kartu kontrol | `20, 92, 280, 32` | Tiga baris `CONTROL_ROWS` dalam **kartu**, bukan pita selebar layar. Memudar setelah `g.controlsTimer` habis (hitung mundur di `update()`, bukan di jalur gambar) |
| Prompt interaksi | `40, 132, 240, 8` | `[F] DESCEND`, `[F] OPEN` dsb., disembunyikan saat modal/pause |
| Readout kamera | `4, 14, 84, 24` | Muncul **hanya** setelah `F6` (`camRig.show = 0` saat boot) — alat yang sengaja bisa dipanggil pemain (lihat BUG-015) |

Bilah vertikal di mana pun adalah 1 unit dari tepi kotak: `panelS` menggambar
border di baris pertama dan terakhir, jadi isi dimulai di `y+2`.

### Kenapa ini tidak bisa melenceng lagi

- `tools/qa/audit-hud.js` (`npm run qa:hud`) memasang kait pada `DS.UI3.quad` dan
  `DS.UI3.text` — satu-satunya dua jalur yang dilalui piksel HUD — menjalankan
  `hud()` untuk dua keadaan (bersih dan penuh), lalu memastikan setiap kotak di
  dalam frame dengan gutter 3 unit, tidak ada dua kotak yang berbagi piksel,
  setiap piksel yang digambar berada di dalam kotak yang dideklarasikan, ketiga
  klaster bawah satu garis dasar, dan vital terpusat di celah antar klaster sudut.
  Angka terukur: 13 kotak, 288 persegi tergambar pada keadaan penuh, dan HUD
  mengklaim 52,5% frame.
- `tools/qa/check-hud-paint.js` (`npm run qa:paint`) mengukur hasil **kompositnya**
  pada 1280x720, 1024x768, dan 1920x1080: memetakan tiap klaster lewat `DS.UI3.view`
  ke piksel halaman dan memastikan ada tinta di dalamnya. Tile mata uang 160x156 px
  di 1280x720 menjadi 240x234 px di 1920x1080 — tepat 1,5x, sesuai skala 4 → 6.
- Label yang bisa memanjang (`clipBody`/`clipMicro`) dipotong ke kotaknya dengan
  `..`, bukan dibiarkan keluar: nama boss dan label tier momentum adalah dua yang
  sudah pernah keluar.

![HUD pada lantai 1: vital di bawah tengah, mata uang kanan atas, skill kanan bawah](docs/img/hud-floor1.png)

## 2.8 Ikon

`[CODE]` HUD, bag, dan menu memakai **sprite yang sudah ada** di `src/art/sprites.js`
(koin, shard, kunci, ikon senjata, ikon skill) dan `src/art/uiart.js` (bingkai,
tombol, ornamen). Tidak ada katalog ikon terpisah: `src/art/icons.js` (309 baris)
pernah ditulis untuk menggantikannya, tidak pernah dipanggil, dan sekarang dihapus.
Ukuran yang dipakai: satu ikon mata uang = 16x16 unit logis, keycap = 8x8.

## 2.9 Kursor

`[CODE]` `src/ui/pointer.js` - kursor digambar sendiri, bukan kursor sistem:

- `CURSOR_PX = 2` dan `unit()` mengembalikan `CURSOR_PX / skala`, sehingga kursor
  berukuran **sama di semua monitor** (ini yang dulu sebabkan kursor raksasa di
  layar FullHD, karena ukurannya dulu ikut skala 6x).
- Bentuknya panah bitmap; `arrowCanvas(accent)` membuat versi beraksen untuk
  keadaan tertentu.
- `Ptr.beginFrame()` mencatat titik tekan dan ambang drag sebelum layar mana pun
  bertanya "apakah diklik".
- Drag item (di bag) dimiliki di sini: `startDrag(item, from)`, `clearDrag()`,
  `dragging()`.

Kursor digambar **sekali** di akhir `main.js` untuk seluruh scene
(`Ptr.drawCursor(current.cursor)`), sehingga tidak ada layar yang bisa lupa
menggambarnya atau menggambarnya dua kali. Scene run memakai `cursor: 'none'`
karena reticle bidik *adalah* kursor di dalam run.

> **Catatan perbaikan.** Kursor sekarang mengikuti ukuran perangkat: pada monitor
> FullHD skala 6, kursor tetap seukuran normal karena digambar dalam **piksel
> perangkat** (bukan unit logis). Inilah jawaban atas keluhan "cursor kekecilan /
> harus zoom".
