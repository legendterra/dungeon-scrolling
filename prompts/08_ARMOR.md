# Batch 8 — Armor Paper Doll (24 overlay + 24 ikon)

> Prompt di bawah sudah FULL-EXPANDED (prefix + ukuran + frame + palette).
> Tinggal copy satu blok utuh, paste ke Claude chat. Satu blok = satu file PNG.

Overlay = potongan armor SAJA, sisanya transparan. Digambar di atas hero 16x16, bukan menggantikan hero.
Warna material (mid / dark / light):

```
cloth    #9b96b8 #514c72 #d8d5e8      bone     #d8d5e8 #6f6a90 #ffffff
leaf     #5cbf62 #1b4436 #a3e86b      iron     #6f6a90 #2a2740 #9b96b8
leather  #8a6340 #2e2018 #b98d5c      gold     #f2c14e #8a7440 #fff0a8
crystal  #4fb3e0 #16324f #a8e4ff      obsidian #7f45b8 #1c1a2b #c86ee0
```

**PALET RESMI GAME — jangan bikin warna baru:**

```
Ink/outline  #0d0b14  #1c1a2b
Stone        #2a2740  #514c72  #6f6a90  #9b96b8  #d8d5e8
Gold         #8a7440  #f2c14e  #fff0a8
Ember        #8a3b2a  #e8743b
Blood        #6e1b28  #c0303c  #8a4550
Green        #1b4436  #2f7d4f  #5cbf62  #a3e86b
Blue         #16324f  #2f6fa8  #4fb3e0  #a8e4ff
Purple       #1e0f2a  #3c2154  #7f45b8  #c86ee0  #a89bff
Wood/leather #2e2018  #5c3f2a  #8a6340  #b98d5c
Skin         #f0c79c
```

---

## ARM-01 Helm — cloth — `arm_helm_cloth.png` (20x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 20x12 pixels. Helmet overlay that sits over the hero head: a soft draped cloth hood. Colors mid/dark/light = #9b96b8 #514c72 #d8d5e8. Distinct readable silhouette at 20 pixels wide. Fully transparent everywhere except the helm itself — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to head rows 4-14. Do NOT draw the head, face or body. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-02 Helm — leaf — `arm_helm_leaf.png` (20x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 20x12 pixels. Helmet overlay that sits over the hero head: a woven leaf circlet with sprigs. Colors mid/dark/light = #5cbf62 #1b4436 #a3e86b. Distinct readable silhouette at 20 pixels wide. Fully transparent everywhere except the helm itself — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to head rows 4-14. Do NOT draw the head, face or body. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-03 Helm — leather — `arm_helm_leather.png` (20x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 20x12 pixels. Helmet overlay that sits over the hero head: a stitched leather cap. Colors mid/dark/light = #8a6340 #2e2018 #b98d5c. Distinct readable silhouette at 20 pixels wide. Fully transparent everywhere except the helm itself — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to head rows 4-14. Do NOT draw the head, face or body. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-04 Helm — bone — `arm_helm_bone.png` (20x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 20x12 pixels. Helmet overlay that sits over the hero head: a horned skull-helm. Colors mid/dark/light = #d8d5e8 #6f6a90 #ffffff. Distinct readable silhouette at 20 pixels wide. Fully transparent everywhere except the helm itself — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to head rows 4-14. Do NOT draw the head, face or body. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-05 Helm — iron — `arm_helm_iron.png` (20x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 20x12 pixels. Helmet overlay that sits over the hero head: a slitted iron great-helm. Colors mid/dark/light = #6f6a90 #2a2740 #9b96b8. Distinct readable silhouette at 20 pixels wide. Fully transparent everywhere except the helm itself — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to head rows 4-14. Do NOT draw the head, face or body. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-06 Helm — gold — `arm_helm_gold.png` (20x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 20x12 pixels. Helmet overlay that sits over the hero head: a crowned gold helm. Colors mid/dark/light = #f2c14e #8a7440 #fff0a8. Distinct readable silhouette at 20 pixels wide. Fully transparent everywhere except the helm itself — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to head rows 4-14. Do NOT draw the head, face or body. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-07 Helm — crystal — `arm_helm_crystal.png` (20x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 20x12 pixels. Helmet overlay that sits over the hero head: a shard tiara. Colors mid/dark/light = #4fb3e0 #16324f #a8e4ff. Distinct readable silhouette at 20 pixels wide. Fully transparent everywhere except the helm itself — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to head rows 4-14. Do NOT draw the head, face or body. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-08 Helm — obsidian — `arm_helm_obsidian.png` (20x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 20x12 pixels. Helmet overlay that sits over the hero head: a spiked obsidian helm. Colors mid/dark/light = #7f45b8 #1c1a2b #c86ee0. Distinct readable silhouette at 20 pixels wide. Fully transparent everywhere except the helm itself — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to head rows 4-14. Do NOT draw the head, face or body. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-09 Chest — cloth — `arm_chest_cloth.png` (24x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 24x16 pixels. Torso armor overlay: pauldrons plus chestplate, a hooded robe with loose sleeves. Colors mid/dark/light = #9b96b8 #514c72 #d8d5e8. Fully transparent everywhere except the armor — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to torso rows 12-26. Do NOT draw the head, arms or legs. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-10 Chest — leaf — `arm_chest_leaf.png` (24x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 24x16 pixels. Torso armor overlay: pauldrons plus chestplate, a leaf-scale mantle with vine shoulder wraps. Colors mid/dark/light = #5cbf62 #1b4436 #a3e86b. Fully transparent everywhere except the armor — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to torso rows 12-26. Do NOT draw the head, arms or legs. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-11 Chest — leather — `arm_chest_leather.png` (24x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 24x16 pixels. Torso armor overlay: pauldrons plus chestplate, a studded leather jerkin with a shoulder strap. Colors mid/dark/light = #8a6340 #2e2018 #b98d5c. Fully transparent everywhere except the armor — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to torso rows 12-26. Do NOT draw the head, arms or legs. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-12 Chest — bone — `arm_chest_bone.png` (24x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 24x16 pixels. Torso armor overlay: pauldrons plus chestplate, a ribcage cuirass with bone pauldrons. Colors mid/dark/light = #d8d5e8 #6f6a90 #ffffff. Fully transparent everywhere except the armor — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to torso rows 12-26. Do NOT draw the head, arms or legs. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-13 Chest — iron — `arm_chest_iron.png` (24x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 24x16 pixels. Torso armor overlay: pauldrons plus chestplate, a riveted iron breastplate with rounded pauldrons. Colors mid/dark/light = #6f6a90 #2a2740 #9b96b8. Fully transparent everywhere except the armor — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to torso rows 12-26. Do NOT draw the head, arms or legs. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-14 Chest — gold — `arm_chest_gold.png` (24x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 24x16 pixels. Torso armor overlay: pauldrons plus chestplate, an ornate gilded cuirass with winged pauldrons. Colors mid/dark/light = #f2c14e #8a7440 #fff0a8. Fully transparent everywhere except the armor — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to torso rows 12-26. Do NOT draw the head, arms or legs. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-15 Chest — crystal — `arm_chest_crystal.png` (24x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 24x16 pixels. Torso armor overlay: pauldrons plus chestplate, a faceted crystal breastplate with shard shoulders. Colors mid/dark/light = #4fb3e0 #16324f #a8e4ff. Fully transparent everywhere except the armor — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to torso rows 12-26. Do NOT draw the head, arms or legs. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-16 Chest — obsidian — `arm_chest_obsidian.png` (24x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 24x16 pixels. Torso armor overlay: pauldrons plus chestplate, a jagged obsidian breastplate with spiked pauldrons. Colors mid/dark/light = #7f45b8 #1c1a2b #c86ee0. Fully transparent everywhere except the armor — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to torso rows 12-26. Do NOT draw the head, arms or legs. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-17 Legs — cloth — `arm_legs_cloth.png` (20x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 20x12 pixels. Greaves and boots overlay: loose cloth wraps and soft shoes. Colors mid/dark/light = #9b96b8 #514c72 #d8d5e8. Fully transparent everywhere except the armor — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to leg rows 24-32. Do NOT draw the torso or head. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-18 Legs — leaf — `arm_legs_leaf.png` (20x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 20x12 pixels. Greaves and boots overlay: leaf-wrapped shins and bare-look sandals. Colors mid/dark/light = #5cbf62 #1b4436 #a3e86b. Fully transparent everywhere except the armor — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to leg rows 24-32. Do NOT draw the torso or head. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-19 Legs — leather — `arm_legs_leather.png` (20x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 20x12 pixels. Greaves and boots overlay: leather greaves and buckled boots. Colors mid/dark/light = #8a6340 #2e2018 #b98d5c. Fully transparent everywhere except the armor — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to leg rows 24-32. Do NOT draw the torso or head. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-20 Legs — bone — `arm_legs_bone.png` (20x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 20x12 pixels. Greaves and boots overlay: bone-plated shins and clawed boots. Colors mid/dark/light = #d8d5e8 #6f6a90 #ffffff. Fully transparent everywhere except the armor — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to leg rows 24-32. Do NOT draw the torso or head. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-21 Legs — iron — `arm_legs_iron.png` (20x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 20x12 pixels. Greaves and boots overlay: plate greaves and heavy sabatons. Colors mid/dark/light = #6f6a90 #2a2740 #9b96b8. Fully transparent everywhere except the armor — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to leg rows 24-32. Do NOT draw the torso or head. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-22 Legs — gold — `arm_legs_gold.png` (20x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 20x12 pixels. Greaves and boots overlay: gilded greaves with filigree and gold boots. Colors mid/dark/light = #f2c14e #8a7440 #fff0a8. Fully transparent everywhere except the armor — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to leg rows 24-32. Do NOT draw the torso or head. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-23 Legs — crystal — `arm_legs_crystal.png` (20x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 20x12 pixels. Greaves and boots overlay: crystalline greaves and faceted boots. Colors mid/dark/light = #4fb3e0 #16324f #a8e4ff. Fully transparent everywhere except the armor — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to leg rows 24-32. Do NOT draw the torso or head. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-24 Legs — obsidian — `arm_legs_obsidian.png` (20x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 20x12 pixels. Greaves and boots overlay: spiked obsidian greaves and heavy boots. Colors mid/dark/light = #7f45b8 #1c1a2b #c86ee0. Fully transparent everywhere except the armor — this is an OVERLAY drawn on top of a 32x32 hero sprite, aligned to leg rows 24-32. Do NOT draw the torso or head. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## ARM-25 Inventory icons (24 file) — `icon_{slot}_{material}.png` (16x16 masing-masing)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 32x32 pixels. Inventory item icon for a piece of dungeon armor: {SLOT: helm / chestplate / greaves} made of {MATERIAL: cloth / leaf / leather / bone / iron / gold / crystal / obsidian}. Single object centered on transparent background, three-quarter presentation, readable at 32px. Use the material colors mid/dark/light exactly as listed in the armor palette. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.

Ulangi 24x: 3 slot x 8 material. Ganti {SLOT} dan {MATERIAL} tiap kali.
```

