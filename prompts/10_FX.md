# Batch 10 — Proyektil, Orb & Elemen (~30 file)

> Prompt di bawah sudah FULL-EXPANDED (prefix + ukuran + frame + palette).
> Tinggal copy satu blok utuh, paste ke Claude chat. Satu blok = satu file PNG.

Elemen: fire #e8743b / ice #4fb3e0 / lightning #f2c14e / poison #5cbf62 / water #2f6fa8 / earth #b98d5c / leaf #a3e86b / dark #7f45b8.

**PALET RESMI GAME — jangan bikin warna baru:**

```
Ink/outline  #0d0b14  #1c1a2b
Stone        #2a2740  #514c72  #6f6a90  #9b96b8  #d8d5e8
Gold         #8a7440  #f2c14e  #fff0a8
Ember        #8a3b2a  #e8743b
Blood        #6e1b28  #c0303c  #8a4550
Green        #1b4436  #2f7d4f  #5cbf62  #a3e86b
Blue         #16324f  #2f6fa8  #4fb3e0  #a8e4ff
Purple       #1e0f2a  #3c2154  #7f45b8  #c86ee0  #a89bff
Wood/leather #2e2018  #5c3f2a  #8a6340  #b98d5c
Skin         #f0c79c
```

---

## FX-01 Orb — fire — `fx_orb_fire.png` (64x16 — 4 frame @16x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 64x16 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 16x16 px, identical pivot and baseline across frames, no gap between frames. Glowing magic orb projectile flying right, bright white #ffffff core with a orange fire corona (#e8743b) and tiny trailing sparks. 4-frame loop: the corona pulses and the sparks rotate; the core never moves. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-01 Orb — ice — `fx_orb_ice.png` (64x16 — 4 frame @16x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 64x16 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 16x16 px, identical pivot and baseline across frames, no gap between frames. Glowing magic orb projectile flying right, bright white #ffffff core with a pale cyan ice corona (#4fb3e0) and tiny trailing sparks. 4-frame loop: the corona pulses and the sparks rotate; the core never moves. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-01 Orb — lightning — `fx_orb_lightning.png` (64x16 — 4 frame @16x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 64x16 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 16x16 px, identical pivot and baseline across frames, no gap between frames. Glowing magic orb projectile flying right, bright white #ffffff core with a yellow lightning corona (#f2c14e) and tiny trailing sparks. 4-frame loop: the corona pulses and the sparks rotate; the core never moves. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-01 Orb — poison — `fx_orb_poison.png` (64x16 — 4 frame @16x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 64x16 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 16x16 px, identical pivot and baseline across frames, no gap between frames. Glowing magic orb projectile flying right, bright white #ffffff core with a green poison corona (#5cbf62) and tiny trailing sparks. 4-frame loop: the corona pulses and the sparks rotate; the core never moves. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-01 Orb — water — `fx_orb_water.png` (64x16 — 4 frame @16x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 64x16 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 16x16 px, identical pivot and baseline across frames, no gap between frames. Glowing magic orb projectile flying right, bright white #ffffff core with a deep blue water corona (#2f6fa8) and tiny trailing sparks. 4-frame loop: the corona pulses and the sparks rotate; the core never moves. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-01 Orb — earth — `fx_orb_earth.png` (64x16 — 4 frame @16x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 64x16 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 16x16 px, identical pivot and baseline across frames, no gap between frames. Glowing magic orb projectile flying right, bright white #ffffff core with a tan earth corona (#b98d5c) and tiny trailing sparks. 4-frame loop: the corona pulses and the sparks rotate; the core never moves. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-01 Orb — leaf — `fx_orb_leaf.png` (64x16 — 4 frame @16x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 64x16 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 16x16 px, identical pivot and baseline across frames, no gap between frames. Glowing magic orb projectile flying right, bright white #ffffff core with a bright leaf green corona (#a3e86b) and tiny trailing sparks. 4-frame loop: the corona pulses and the sparks rotate; the core never moves. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-01 Orb — dark — `fx_orb_dark.png` (64x16 — 4 frame @16x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 64x16 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 16x16 px, identical pivot and baseline across frames, no gap between frames. Glowing magic orb projectile flying right, bright white #ffffff core with a violet dark corona (#7f45b8) and tiny trailing sparks. 4-frame loop: the corona pulses and the sparks rotate; the core never moves. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-02 Impact burst — fire — `fx_burst_fire.png` (128x32 — 4 frame @32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 128x32 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 32x32 px, identical pivot and baseline across frames, no gap between frames. Radial impact burst, 4 frames: frame 1 small bright flash, frame 2 expanding ring of shards and sparks, frame 3 wider and thinner, frame 4 sparse fading motes. orange fire (#e8743b) with a white #ffffff hot centre in the first two frames. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-02 Impact burst — ice — `fx_burst_ice.png` (128x32 — 4 frame @32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 128x32 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 32x32 px, identical pivot and baseline across frames, no gap between frames. Radial impact burst, 4 frames: frame 1 small bright flash, frame 2 expanding ring of shards and sparks, frame 3 wider and thinner, frame 4 sparse fading motes. pale cyan ice (#4fb3e0) with a white #ffffff hot centre in the first two frames. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-02 Impact burst — lightning — `fx_burst_lightning.png` (128x32 — 4 frame @32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 128x32 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 32x32 px, identical pivot and baseline across frames, no gap between frames. Radial impact burst, 4 frames: frame 1 small bright flash, frame 2 expanding ring of shards and sparks, frame 3 wider and thinner, frame 4 sparse fading motes. yellow lightning (#f2c14e) with a white #ffffff hot centre in the first two frames. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-02 Impact burst — poison — `fx_burst_poison.png` (128x32 — 4 frame @32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 128x32 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 32x32 px, identical pivot and baseline across frames, no gap between frames. Radial impact burst, 4 frames: frame 1 small bright flash, frame 2 expanding ring of shards and sparks, frame 3 wider and thinner, frame 4 sparse fading motes. green poison (#5cbf62) with a white #ffffff hot centre in the first two frames. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-02 Impact burst — water — `fx_burst_water.png` (128x32 — 4 frame @32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 128x32 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 32x32 px, identical pivot and baseline across frames, no gap between frames. Radial impact burst, 4 frames: frame 1 small bright flash, frame 2 expanding ring of shards and sparks, frame 3 wider and thinner, frame 4 sparse fading motes. deep blue water (#2f6fa8) with a white #ffffff hot centre in the first two frames. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-02 Impact burst — earth — `fx_burst_earth.png` (128x32 — 4 frame @32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 128x32 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 32x32 px, identical pivot and baseline across frames, no gap between frames. Radial impact burst, 4 frames: frame 1 small bright flash, frame 2 expanding ring of shards and sparks, frame 3 wider and thinner, frame 4 sparse fading motes. tan earth (#b98d5c) with a white #ffffff hot centre in the first two frames. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-02 Impact burst — leaf — `fx_burst_leaf.png` (128x32 — 4 frame @32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 128x32 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 32x32 px, identical pivot and baseline across frames, no gap between frames. Radial impact burst, 4 frames: frame 1 small bright flash, frame 2 expanding ring of shards and sparks, frame 3 wider and thinner, frame 4 sparse fading motes. bright leaf green (#a3e86b) with a white #ffffff hot centre in the first two frames. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-02 Impact burst — dark — `fx_burst_dark.png` (128x32 — 4 frame @32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 128x32 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 32x32 px, identical pivot and baseline across frames, no gap between frames. Radial impact burst, 4 frames: frame 1 small bright flash, frame 2 expanding ring of shards and sparks, frame 3 wider and thinner, frame 4 sparse fading motes. violet dark (#7f45b8) with a white #ffffff hot centre in the first two frames. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-03 Fire pool — `fx_firepool.png` (192x20 — 4 frame @48x20)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 192x20 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 48x20 px, identical pivot and baseline across frames, no gap between frames. Ground fire patch loop, 4 frames of low licking flames along a flat ground line, flames never exceed the frame height. Palette #8a3b2a #e8743b #f2c14e #fff0a8. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-04 Poison cloud — `fx_poisoncloud.png` (256x40 — 4 frame @64x40)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 256x40 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 64x40 px, identical pivot and baseline across frames, no gap between frames. Drifting green gas cloud, 4-frame loop, soft dithered checkerboard edge for the fade (no blur), the mass slowly churning. Palette #1b4436 #2f7d4f #5cbf62 #a3e86b. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-05 Water puddle — `fx_puddle.png` (96x12 — 2 frame @48x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 96x12 pixels. Horizontal sprite sheet, 2 frames in a single row, each frame exactly 48x12 px, identical pivot and baseline across frames, no gap between frames. Shallow water pool on the ground, 2-frame ripple loop, flat elliptical shape with a highlight line. Palette #16324f #2f6fa8 #4fb3e0 #a8e4ff. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-06 Ice patch — `fx_icepatch.png` (96x12 — 2 frame @48x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 96x12 pixels. Horizontal sprite sheet, 2 frames in a single row, each frame exactly 48x12 px, identical pivot and baseline across frames, no gap between frames. Frozen slick floor patch with frost crystals along the edges, 2-frame subtle shimmer loop. Palette #16324f #4fb3e0 #a8e4ff #ffffff. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-07 Steam cloud — `fx_steam.png` (256x40 — 4 frame @64x40)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 256x40 pixels. Horizontal sprite sheet, 4 frames in a single row, each frame exactly 64x40 px, identical pivot and baseline across frames, no gap between frames. White-grey steam plume rising, 4-frame loop, dense enough to be blinding, dithered edges. Palette #6f6a90 #9b96b8 #d8d5e8 #ffffff. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-08 Lightning bolt — `fx_lightning.png` (48x96 — 3 frame @16x96)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 48x96 pixels. Horizontal sprite sheet, 3 frames in a single row, each frame exactly 16x96 px, identical pivot and baseline across frames, no gap between frames. Vertical jagged lightning strike from the top edge to the bottom, 3 frames: frame 1 thin forking leader, frame 2 thick bright main bolt with a white core, frame 3 fading afterglow. Palette #fff0a8 #f2c14e #ffffff. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## FX-09 Explosion — `fx_explosion.png` (384x64 — 6 frame @64x64)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 384x64 pixels. Horizontal sprite sheet, 6 frames in a single row, each frame exactly 64x64 px, identical pivot and baseline across frames, no gap between frames. Bomber death explosion, 6 frames: white flash, expanding fireball, peak fireball with debris, collapsing, smoke ball, sparse dissipating smoke. Palette #fff0a8 #f2c14e #e8743b #8a3b2a #514c72 #9b96b8. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

