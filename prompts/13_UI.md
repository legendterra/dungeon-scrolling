# Batch 13 — UI (~50 file)

> Prompt di bawah sudah FULL-EXPANDED (prefix + ukuran + frame + palette).
> Tinggal copy satu blok utuh, paste ke Claude chat. Satu blok = satu file PNG.

Gaya UI: pixelated, minimalis, sesuai arah desain yang sekarang.

**PALET RESMI GAME — jangan bikin warna baru:**

```
Ink/outline  #0d0b14  #1c1a2b
Stone        #2a2740  #514c72  #6f6a90  #9b96b8  #d8d5e8
Gold         #8a7440  #f2c14e  #fff0a8
Ember        #8a3b2a  #e8743b
Blood        #6e1b28  #c0303c  #8a4550
Green        #1b4436  #2f7d4f  #5cbf62  #a3e86b
Blue         #16324f  #2f6fa8  #4fb3e0  #a8e4ff
Purple       #1e0f2a  #3c2154  #7f45b8  #c86ee0  #a89bff
Wood/leather #2e2018  #5c3f2a  #8a6340  #b98d5c
Skin         #f0c79c
```

---

## UI-01 Key cap — `ui_keycap.png` (56x28 — 2 frame @28x28)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 56x28 pixels. Horizontal sprite sheet, 2 frames in a single row, each frame exactly 28x28 px, identical pivot and baseline across frames, no gap between frames. Minimal pixel keyboard keycap with an EMPTY blank face (no letter — the engine draws the letter), 2px light top-left bevel and 2px dark bottom-right bevel. 2 states side by side: frame 1 idle, frame 2 pressed (face shifted 2 pixels down, overall darker, bevel inverted). Palette #1c1a2b #514c72 #9b96b8. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-02 Key cap wide — `ui_keycap_wide.png` (112x28 — 2 frame @56x28)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 112x28 pixels. Horizontal sprite sheet, 2 frames in a single row, each frame exactly 56x28 px, identical pivot and baseline across frames, no gap between frames. Wide pixel keyboard keycap for SHIFT / SPACE with an EMPTY blank face, 2px light top-left bevel and 2px dark bottom-right bevel. 2 states side by side: idle, and pressed (shifted 2 pixels down, darker). Palette #1c1a2b #514c72 #9b96b8. Same visual language and bevel thickness as the 14x14 keycap. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-03 Health bar frame — `ui_hpframe.png` (128x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 128x16 pixels. Empty pixel HP bar FRAME only (no fill — the engine draws the red fill inside), horizontal, notched into segments by 2px vertical dividers, iron and bone styling with small end caps. Interior must be transparent. Palette #1c1a2b #6f6a90 #d8d5e8. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-04 Mana bar frame — `ui_mpframe.png` (128x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 128x12 pixels. Empty pixel mana bar FRAME only (no fill), horizontal, notched segments, thinner than the HP bar, blue-tinted iron styling with end caps. Interior transparent. Palette #1c1a2b #2f6fa8 #a8e4ff. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-05 Skill slot frame — `ui_skillslot.png` (40x40)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 40x40 pixels. Empty square skill slot frame, 4px border with a beveled inner shadow and a rivet in each corner, hollow transparent centre so an icon and a cooldown sweep can be drawn inside. Palette #1c1a2b #2a2740 #6f6a90 #9b96b8. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-06 Weapon display panel — `ui_weaponpanel.png` (96x64)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 96x64 pixels. Small pixel plaque frame for a bottom-left weapon display, thin ornate border with corner flourishes, hollow transparent centre. Palette #1c1a2b #2a2740 #8a7440 #f2c14e. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-07 Boon icon — bloodthirst — `ui_boon_bloodthirst.png` (32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 32x32 pixels. Single-symbol pixel UI icon: a fanged mouth over a blood drop. Monochrome light grey #9b96b8 with dark #1c1a2b outline, plus ONE accent color #c0303c used sparingly on the key detail. Bold simple silhouette, must stay readable at 32px. Centered, transparent background, no frame or border. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-07 Boon icon — ironhide — `ui_boon_ironhide.png` (32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 32x32 pixels. Single-symbol pixel UI icon: a riveted iron shield. Monochrome light grey #9b96b8 with dark #1c1a2b outline, plus ONE accent color #9b96b8 used sparingly on the key detail. Bold simple silhouette, must stay readable at 32px. Centered, transparent background, no frame or border. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-07 Boon icon — featherweight — `ui_boon_featherweight.png` (32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 32x32 pixels. Single-symbol pixel UI icon: a single feather. Monochrome light grey #9b96b8 with dark #1c1a2b outline, plus ONE accent color #a8e4ff used sparingly on the key detail. Bold simple silhouette, must stay readable at 32px. Centered, transparent background, no frame or border. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-07 Boon icon — sharpshooter — `ui_boon_sharpshooter.png` (32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 32x32 pixels. Single-symbol pixel UI icon: an arrow hitting a bullseye. Monochrome light grey #9b96b8 with dark #1c1a2b outline, plus ONE accent color #5cbf62 used sparingly on the key detail. Bold simple silhouette, must stay readable at 32px. Centered, transparent background, no frame or border. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-07 Boon icon — overflow — `ui_boon_overflow.png` (32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 32x32 pixels. Single-symbol pixel UI icon: a flask overflowing with liquid. Monochrome light grey #9b96b8 with dark #1c1a2b outline, plus ONE accent color #4fb3e0 used sparingly on the key detail. Bold simple silhouette, must stay readable at 32px. Centered, transparent background, no frame or border. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-07 Boon icon — conduit — `ui_boon_conduit.png` (32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 32x32 pixels. Single-symbol pixel UI icon: a lightning bolt between two nodes. Monochrome light grey #9b96b8 with dark #1c1a2b outline, plus ONE accent color #f2c14e used sparingly on the key detail. Bold simple silhouette, must stay readable at 32px. Centered, transparent background, no frame or border. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-07 Boon icon — golden touch — `ui_boon_golden_touch.png` (32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 32x32 pixels. Single-symbol pixel UI icon: an open hand with a coin above it. Monochrome light grey #9b96b8 with dark #1c1a2b outline, plus ONE accent color #f2c14e used sparingly on the key detail. Bold simple silhouette, must stay readable at 32px. Centered, transparent background, no frame or border. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-07 Boon icon — berserker — `ui_boon_berserker.png` (32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 32x32 pixels. Single-symbol pixel UI icon: two crossed axes with rage marks. Monochrome light grey #9b96b8 with dark #1c1a2b outline, plus ONE accent color #e8743b used sparingly on the key detail. Bold simple silhouette, must stay readable at 32px. Centered, transparent background, no frame or border. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-07 Boon icon — momentum — `ui_boon_momentum.png` (32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 32x32 pixels. Single-symbol pixel UI icon: three forward speed lines with an arrow. Monochrome light grey #9b96b8 with dark #1c1a2b outline, plus ONE accent color #a3e86b used sparingly on the key detail. Bold simple silhouette, must stay readable at 32px. Centered, transparent background, no frame or border. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-07 Boon icon — blood harvest — `ui_boon_blood_harvest.png` (32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 32x32 pixels. Single-symbol pixel UI icon: a sickle over a blood drop. Monochrome light grey #9b96b8 with dark #1c1a2b outline, plus ONE accent color #6e1b28 used sparingly on the key detail. Bold simple silhouette, must stay readable at 32px. Centered, transparent background, no frame or border. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-07 Boon icon — fleetfoot — `ui_boon_fleetfoot.png` (32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 32x32 pixels. Single-symbol pixel UI icon: a winged boot. Monochrome light grey #9b96b8 with dark #1c1a2b outline, plus ONE accent color #a3e86b used sparingly on the key detail. Bold simple silhouette, must stay readable at 32px. Centered, transparent background, no frame or border. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-07 Boon icon — ricochet — `ui_boon_ricochet.png` (32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 32x32 pixels. Single-symbol pixel UI icon: an arrow bouncing off an angled wall. Monochrome light grey #9b96b8 with dark #1c1a2b outline, plus ONE accent color #4fb3e0 used sparingly on the key detail. Bold simple silhouette, must stay readable at 32px. Centered, transparent background, no frame or border. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-07 Boon icon — vigil — `ui_boon_vigil.png` (32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 32x32 pixels. Single-symbol pixel UI icon: an open watchful eye. Monochrome light grey #9b96b8 with dark #1c1a2b outline, plus ONE accent color #fff0a8 used sparingly on the key detail. Bold simple silhouette, must stay readable at 32px. Centered, transparent background, no frame or border. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-07 Boon icon — ruin — `ui_boon_ruin.png` (32x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 32x32 pixels. Single-symbol pixel UI icon: a cracked skull. Monochrome light grey #9b96b8 with dark #1c1a2b outline, plus ONE accent color #7f45b8 used sparingly on the key detail. Bold simple silhouette, must stay readable at 32px. Centered, transparent background, no frame or border. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-08 Element affix icon — fire — `ui_elem_fire.png` (24x24)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 24x24 pixels. Tiny single-symbol pixel UI icon: a flame, in #e8743b with a dark #1c1a2b outline and one lighter highlight of the same hue. Extremely simple silhouette, readable at 24px. Centered, transparent background. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-08 Element affix icon — ice — `ui_elem_ice.png` (24x24)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 24x24 pixels. Tiny single-symbol pixel UI icon: a snowflake, in #4fb3e0 with a dark #1c1a2b outline and one lighter highlight of the same hue. Extremely simple silhouette, readable at 24px. Centered, transparent background. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-08 Element affix icon — lightning — `ui_elem_lightning.png` (24x24)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 24x24 pixels. Tiny single-symbol pixel UI icon: a lightning bolt, in #f2c14e with a dark #1c1a2b outline and one lighter highlight of the same hue. Extremely simple silhouette, readable at 24px. Centered, transparent background. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-08 Element affix icon — poison — `ui_elem_poison.png` (24x24)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 24x24 pixels. Tiny single-symbol pixel UI icon: a bubbling droplet with a skull hint, in #5cbf62 with a dark #1c1a2b outline and one lighter highlight of the same hue. Extremely simple silhouette, readable at 24px. Centered, transparent background. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-08 Element affix icon — water — `ui_elem_water.png` (24x24)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 24x24 pixels. Tiny single-symbol pixel UI icon: a water droplet with a ripple, in #2f6fa8 with a dark #1c1a2b outline and one lighter highlight of the same hue. Extremely simple silhouette, readable at 24px. Centered, transparent background. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-08 Element affix icon — earth — `ui_elem_earth.png` (24x24)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 24x24 pixels. Tiny single-symbol pixel UI icon: a jagged rock, in #b98d5c with a dark #1c1a2b outline and one lighter highlight of the same hue. Extremely simple silhouette, readable at 24px. Centered, transparent background. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-08 Element affix icon — leaf — `ui_elem_leaf.png` (24x24)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 24x24 pixels. Tiny single-symbol pixel UI icon: a leaf, in #a3e86b with a dark #1c1a2b outline and one lighter highlight of the same hue. Extremely simple silhouette, readable at 24px. Centered, transparent background. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-08 Element affix icon — dark — `ui_elem_dark.png` (24x24)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 24x24 pixels. Tiny single-symbol pixel UI icon: a crescent void, in #7f45b8 with a dark #1c1a2b outline and one lighter highlight of the same hue. Extremely simple silhouette, readable at 24px. Centered, transparent background. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-09 Rarity gem — common — `ui_gem_common.png` (12x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 12x12 pixels. Tiny faceted gem for an item list row, diamond shape, main color #9b96b8 with a darker shadow of the same hue and a 2x2 white #ffffff highlight block. Transparent background. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-09 Rarity gem — uncommon — `ui_gem_uncommon.png` (12x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 12x12 pixels. Tiny faceted gem for an item list row, diamond shape, main color #5cbf62 with a darker shadow of the same hue and a 2x2 white #ffffff highlight block. Transparent background. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-09 Rarity gem — rare — `ui_gem_rare.png` (12x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 12x12 pixels. Tiny faceted gem for an item list row, diamond shape, main color #4fb3e0 with a darker shadow of the same hue and a 2x2 white #ffffff highlight block. Transparent background. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-09 Rarity gem — epic — `ui_gem_epic.png` (12x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 12x12 pixels. Tiny faceted gem for an item list row, diamond shape, main color #c86ee0 with a darker shadow of the same hue and a 2x2 white #ffffff highlight block. Transparent background. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-09 Rarity gem — legendary — `ui_gem_legendary.png` (12x12)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 12x12 pixels. Tiny faceted gem for an item list row, diamond shape, main color #f2c14e with a darker shadow of the same hue and a 2x2 white #ffffff highlight block. Transparent background. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-10 Cursor — `ui_cursor.png` (32x16 — 2 frame @16x16)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 32x16 pixels. Horizontal sprite sheet, 2 frames in a single row, each frame exactly 16x16 px, identical pivot and baseline across frames, no gap between frames. Two pixel cursors side by side: frame 1 a classic arrow pointer, frame 2 a pointing hand. Both white #ffffff fill with a hard #0d0b14 outline, hotspot at the top-left 2x2 pixel block. Transparent background. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-11 Minimap tiles — `ui_minimap.png` (40x8 — 5 frame @8x8)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 40x8 pixels. Horizontal sprite sheet, 5 frames in a single row, each frame exactly 8x8 px, identical pivot and baseline across frames, no gap between frames. Five 8x8 pixel minimap markers in a row: frame 1 room (filled square #6f6a90), frame 2 corridor (thin horizontal bar #514c72), frame 3 boss (square with a red #c0303c 2x2 centre block), frame 4 shop (square with a gold #f2c14e 2x2 centre block), frame 5 current position (bright white #ffffff square). Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-12 Title logo — `ui_logo.png` (480x128)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 480x128 pixels. Pixel wordmark logo reading exactly "DUNGEON SCROLLING" on two lines, carved stone letters with a gold #f2c14e edge light on the top edges, cracks and chips in the stone, dungeon fantasy. Letters must be clean and fully legible, correct spelling, no extra words. Palette #1c1a2b #2a2740 #6f6a90 #9b96b8 #8a7440 #f2c14e #fff0a8. Transparent background. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

## UI-13 Depth banner — `ui_banner.png` (256x32)

```text
Pixel art sprite, strict 1:1 pixel grid, no anti-aliasing, no blur, no gradients, hard-edged pixels only, transparent PNG background (alpha), no drop shadow, no text, no watermark, no border frame, single object centered, orthographic side view, dark-fantasy dungeon crawler style, limited palette (max 12 colors), crisp near-black outline (#0d0b14). Output exactly 256x32 pixels. Ornate pixel plaque for a floor-name banner, wide and short, thin gold-trimmed stone frame with small end flourishes, hollow transparent centre so text can be drawn inside. Palette #1c1a2b #2a2740 #8a7440 #f2c14e. Design for a 640x360 game screen at tile size 32x32: the silhouette must read clearly at 100% zoom.
```

